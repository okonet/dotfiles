The <strong><code>json()</code></strong> method of the <a href="/en-US/docs/Web/API/Body"><code>Body</code></a> mixin takes a <a href="/en-US/docs/Web/API/Response"><code>Response</code></a> stream and reads it to completion. It returns a promise that resolves with the result of parsing the body text as <a href="/en-US/docs/Web/JavaScript/Reference/Global_Objects/JSON"><code>JSON</code></a>.